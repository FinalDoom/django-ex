// JavaScript Document

function init( ) {
//    initColorBox( );
}

function toggleDisplay( id ) {
	document.getElementById( id ).style.display = ( document.getElementById( id ).style.display == "none" ) ? "list-item" : "none";
}
